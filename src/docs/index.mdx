---
name: ColorPicker
route: /
---
import { Playground, Props } from 'docz'
import ColorPicker from '../components/sketch'

# ColorPicker

## Basic usage

<Playground>
  {() => (
    <ColorPicker
      onChange={(color) => console.log(color)}
    />
  )}
</Playground>
